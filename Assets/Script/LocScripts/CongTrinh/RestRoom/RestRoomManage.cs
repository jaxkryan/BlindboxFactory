using UnityEngine;

public class RestRoomManage : MonoBehaviour
{
    public float numberOfBedMax = 7;
    public float energyRestorePerSec = 10;
    public float numberOfBedRemain = 7;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
