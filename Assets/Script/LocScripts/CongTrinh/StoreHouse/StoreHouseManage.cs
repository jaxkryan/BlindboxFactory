using UnityEngine;

public class StoreHouseManage : MonoBehaviour
{
    public float storageMaxCapacity;
    public float storageCurrentCapacity;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
